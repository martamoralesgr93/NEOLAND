

import { initControler, initTemplate } from "./src/utils";
import "./style.css";


/** creamos el template inicial con la estructura basica como es el footer
 * main y el header con el contenido con su componente correspondiente
 */

initTemplate();


/** no le metemos parametro en el initControler 
 */

initControler();